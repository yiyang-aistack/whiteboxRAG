# Development / CI dependencies for whiteBoxRAG
#
# The service itself only needs requirements.txt. Install this file on top of it
# when you want to run the test suite or the linter:
#
#   pip install -r requirements.txt -r requirements-dev.txt
#
# Note: every file under tests/ is runnable as a plain script
# (python tests/test_parser.py), so pytest is convenient but not mandatory.

# Testing
pytest==8.0.0
pytest-asyncio==0.23.4

# Linting
flake8>=7.0.0
